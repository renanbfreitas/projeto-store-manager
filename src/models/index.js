const productModel = require('./product.model');
const salesProductModel = require('./addsalesproduct.model');
const salesModel = require('./sales.model');

module.exports = {
  productModel,
  salesProductModel,
  salesModel,
};
